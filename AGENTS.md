# Code Documentation Specification

本仓库同时包含前端（Vue 3 + TypeScript）与后端（Java / Spring Boot）代码。为避免规则混用，请按“修改文件所在目录”选择对应规范。

## 适用范围（按路径分流）

- 前端（Vue 3 + TypeScript）代码：当你修改路径命中以下目录时，必须遵循前端规范  
  `d:\my_document\program_product\match_system\伙伴匹配中心源码\match_project\yupao-frontend-master\yupao-frontend-master\`  
  对应规则文件：同目录下的 `AGENTS.md`
- 后端（Java / Spring Boot）代码：当你修改路径命中以下目录时，必须遵循后端规范  
  `d:\my_document\program_product\match_system\伙伴匹配中心源码\match_project\yupao-backend-master\yupao-backend-master\`  
  对应规则文件：同目录下的 `AGENTS.md`
